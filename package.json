{
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter playground dev",
    "check": "pnpm -r run check",
    "version": "changeset version",
    "release": "bash .github/scripts/release.sh",
    "changeset:patch": "node .github/scripts/add-changeset.mjs patch",
    "changeset:minor": "node .github/scripts/add-changeset.mjs minor",
    "changeset:major": "node .github/scripts/add-changeset.mjs major"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
