{
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter playground dev",
    "check": "pnpm -r run check"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
